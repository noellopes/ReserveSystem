@model IEnumerable<ReserveSystem.Models.TypeOfSchedule>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeOfScheduleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeOfScheduleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JobDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TypeOfScheduleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TypeOfScheduleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TypeOfScheduleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
