@model IEnumerable<ReserveSystem.Models.JobTestModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Job_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job_Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Job_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Job_Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Job_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Job_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Job_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
