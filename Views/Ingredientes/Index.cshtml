@model IEnumerable<ReserveSystem.Models.Ingrediente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuantidadeDisponivel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UnidadeMedida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuantidadeDisponivel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UnidadeMedida)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IngredienteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IngredienteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IngredienteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
