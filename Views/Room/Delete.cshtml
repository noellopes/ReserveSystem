@model ReserveSystem.Models.RoomModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RoomModel</h4>
    <hr />
    <dl class="row">
        <!-- Exibindo o tipo de quarto -->
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RoomType)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RoomType.Type)  <!-- Acesso ao nome do tipo de quarto -->
        </dd>

        <!-- Exibindo a capacidade do quarto -->
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RoomType.RoomCapacity)  <!-- Acesso à capacidade no RoomType -->
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RoomType.RoomCapacity)  <!-- Exibindo a capacidade do quarto -->
        </dd>
        <!-- Exibindo se o quarto tem vista -->
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RoomType.HasView)  <!-- Acesso à vista no RoomType -->
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RoomType.HasView)  <!-- Exibindo se o quarto tem vista -->
        </dd>

        <!-- Exibindo se o quarto é adaptado -->
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RoomType.AcessibilityRoom)  <!-- Acesso ao RoomType para a adaptação -->
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RoomType.AcessibilityRoom)  <!-- Exibindo se o quarto é adaptado -->
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="RoomTypeId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
