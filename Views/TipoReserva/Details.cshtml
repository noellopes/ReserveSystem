@model ReserveSystem.Models.TipoReserva

@{
    ViewData["Title"] = "Booking Type Details";
}

<h2>Booking Type Details</h2>

<div>
    <h4>Booking Type</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.NomeReserva)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.NomeReserva)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.idTipoReserva" class="btn btn-primary">Edit</a> |
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
</div>
