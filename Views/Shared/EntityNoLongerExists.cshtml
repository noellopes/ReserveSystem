<h1>@ViewBag.Title</h1>
<div class="container mt-5">
    <div class="alert alert-warning" role="alert">
        <h4 class="alert-heading">Warning!</h4>
        <p>The @ViewBag.Entity no longer exists or it might have been deleted</p>
        <hr>
        <a asp-action="@ViewBag.Action" asp-controller="@ViewBag.Controller" class="btn btn-primary justify-content-end">Back to list</a>
    </div>
</div>